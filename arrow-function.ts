function addNew(num1: number, num2: number): number {
  return num1 + num2;
}

const add2 = (num1: number, num2: number): number => num1 + num2;
const doMatch = (number: number): void => {
  console.log(2 * number);
};
